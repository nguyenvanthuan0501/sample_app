<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render t("form_error_count.render_unfollow") %>
  <% else %>
    <%= render t("form_error_count.render_follow") %>
  <% end %>
  </div>
<% end %>
