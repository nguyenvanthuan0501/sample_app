<li>
  <%= gravatar_for user, size: Settings.user.app.views.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("static_pages.edit.delete"), user,
      method: :delete, data: {confirm: t("notification.three")} %>
  <% end %>
</li>
